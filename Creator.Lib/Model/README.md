# Configurate the cmd line tool
By sending command via command line e.g creator -c createSolutionA.json

Where SolutionA is and serialized json command representing the command

{
	"Command": {
		"Id": "CreateSolutionA",
		"Model":{
			
		}
	}
}